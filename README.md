# David's Project

Welcome to my project. To get set up please follow the instructions below:

First clone down this project using:

```sh
git clone <url>
```

Install local dependencies using:

```sh
npm install
```

## env variables

In order to run this project locally you will need a `.env` file at the root level of the project. Message David for the contents.
